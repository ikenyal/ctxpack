{
  "version": "v1",
  "hooks": [
    {
      "name": "tests on core change",
      "trigger": "PostFileSave",
      "description": "Run vitest when a file under src/core/ is saved",
      "matcher": "src/core/",
      "action": {
        "type": "command",
        "command": "echo \"$(date -u +%Y-%m-%dT%H:%M:%SZ) tests on core change\" >> .kiro/hook-runs.log && npx vitest run"
      },
      "enabled": true
    }
  ]
}